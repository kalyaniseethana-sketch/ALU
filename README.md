# ALU
An **Arithmetic Logic Unit (ALU)** is a digital circuit that performs arithmetic and logical operations on binary data. This project demonstrates ALU operations such as addition, subtraction, AND, OR, and XOR based on the selected operation.
